# Lock = capa viva en Quickshell (ver Scripts/hypr/lock.sh y .config/quickshell/lock).
$LOCKSCREEN = $HOME/dotfiles/Scripts/hypr/lock.sh

general {
    lock_cmd = $LOCKSCREEN
    unlock_cmd =
    before_sleep_cmd = $LOCKSCREEN
    after_sleep_cmd =
    ignore_dbus_inhibit = 0
}
